@ This example shows how runtime variables work @

use system
use self

[
    version 3.0
    output runtime
    os linux
    arch amd64
    obfuscate false
    debugger_behavior stop
]

loop main_func {
    system::outln("€666€") @ This will print the name of the current running user @
    system::exit("0")
}