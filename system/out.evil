@ Showcases how you print a message @

use system

[
    version 3.0
    output out
    os linux
    arch amd64
    obfuscate false
    debugger_behavior stop
]



loop main_func {
    system::outln("Hello with a newline!") @ Adds a new line at the end @
    system::out("Hello without a newline!") 
    system::exit("0")
}